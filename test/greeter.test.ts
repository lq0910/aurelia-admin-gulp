/// <refrence path="../source/interfaces/interfaces.d.ts" />

import { expect } from "chai";

describe("Greeter Class", () => {

    it("Should set msg when an instance is    created", () => {
        let expected = "world!";
        expect(expected).eql(expected);
    });

});
